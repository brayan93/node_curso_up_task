<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        style
            include ./email.css
    body
        h2 Recuperar contraseña
        p Hola, Has solicitado reestablecer su contraseña, haz click en el sigueinte enlace, este enlace es temporal.
        a(href=`${resetUrl}`) Ir a Resetear la contraseña
        p Si no puedes acceder a este enlace, visita #{resetUrl}
        p Si no lo solicitaste, ignora este correo