<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 #{nombrePagina}
            p Ingrese su E-mail para recuperar la contraseña
            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error
            form.caja-login(action="/reestablecer" method="POST")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" value= email placeholder="Tu Email")
                .campo.enviar
                    input(type="submit" value="Recuperar contraseña" class="boton")
                .campo.acciones
                    a(href="/iniciar-sesion") Regresar a iniciar sesion