<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login
        .contenedor-formulario
            h1 Crea tu cuenta en UpTask

            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form#formulario.caja.caja-login(action="/crear-cuenta" method="POST")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" value= email placeholder="Tu Email" required)
                .campo
                    label(for="password") Contraseña:
                    input(type="password" name="password" value= password placeholder="Tu Contraseña" required)
                .campo.enviar
                    input(type="submit" value="Crear Cuenta" class="boton")
                .campo
                    a(href="/iniciar-sesion") Volver a Iniciar Sesión
        script(src="/dist/bundle.js")